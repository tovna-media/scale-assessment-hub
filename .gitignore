# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local
# Note: .env / .env.development are committed but hold ONLY public client keys
# (Supabase URL + anon key, Stripe publishable key). Real secrets live in the
# hosting env. Local secret overrides go in .env*.local, ignored by *.local above.

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
